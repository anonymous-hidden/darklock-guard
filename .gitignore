node_modules/
.env
.env.local
.env.*.local
config.json
logs/*.log
temp/
uploads/
.DS_Store
Thumbs.db
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm
.eslintcache
.nyc_output
coverage/
.coverage
.cache
.parcel-cache
dist/
build/
.venv/

# Documentation (keep README and code only)
*.md
!README.md

# ─── RUNTIME / MUTABLE DATA (never overwrite on deploy) ────────────────────────
# All SQLite databases and their WAL/SHM companion files
data/*.db
data/*.db-shm
data/*.db-wal
data/*.db.backup
data/*.backup
darklock/data/*.db
darklock/data/*.db-shm
darklock/data/*.db-wal

# Dynamic JSON data files written by the bot at runtime
data/*.json
data/snapshots/

# User session and account data written at runtime
darklock/data/sessions.json
darklock/data/users.json

# ─── APP RELEASE BINARIES (uploaded separately, not via git) ───────────────────
darklock/downloads/*.deb
darklock/downloads/*.AppImage
darklock/downloads/*.exe
darklock/downloads/*.msi
darklock/downloads/*.tar.gz
darklock/downloads/*.zip
darklock/downloads/*.dmg
darklock/downloads/*.pkg

# Security files (local only)
SECURITY_INTEGRATION_GUIDE.js
file-protection/backups/
file-protection/logs/
file-protection/config/baseline.json

# Build artifacts
ainti-tampering-app/tauri-app/src-tauri/target/
ainti-tampering-app/tauri-app/node_modules/
guard-v2/target/
guard-v2/desktop/target/

# Archives and large files
*.tar.gz
*.zip
*.tar
*.gz
__pycache__/
